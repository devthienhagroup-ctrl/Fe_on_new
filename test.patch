diff --git a/src/components/host/CreateHostModal.vue b/src/components/host/CreateHostModal.vue
new file mode 100644
index 0000000000000000000000000000000000000000..2d27b89d0706a6b6c5d7bf562c29d270d1ee311f
--- /dev/null
+++ b/src/components/host/CreateHostModal.vue
@@ -0,0 +1,332 @@
+<template>
+  <div
+      class="modal fade show d-block"
+      tabindex="-1"
+      role="dialog"
+      @click.self="handleClose"
+  >
+    <div class="modal-dialog modal-lg modal-dialog-centered" role="document">
+      <div class="modal-content">
+        <div class="modal-header border-0 pb-0">
+          <h5 class="modal-title fw-bold">
+            <i class="fas fa-user-plus me-2 text-primary"></i>
+            Tạo Host mới
+          </h5>
+          <button type="button" class="btn-close" aria-label="Close" @click="handleClose"></button>
+        </div>
+
+        <div class="modal-body pt-3">
+          <div class="row g-4">
+            <div class="col-md-8">
+              <div class="card border-0 shadow-sm h-100">
+                <div class="card-body">
+                  <h6 class="fw-semibold mb-3 text-primary">
+                    <i class="fas fa-id-card me-2"></i>
+                    Thông tin cơ bản
+                  </h6>
+
+                  <div class="mb-3">
+                    <label class="form-label">Email <span class="text-danger">*</span></label>
+                    <input
+                        v-model.trim="email"
+                        type="email"
+                        class="form-control"
+                        placeholder="email@example.com"
+                    />
+                    <div v-if="emailError" class="text-danger small mt-1">{{ emailError }}</div>
+                  </div>
+
+                  <div class="mb-3">
+                    <label class="form-label">Họ và tên <span class="text-danger">*</span></label>
+                    <input
+                        v-model.trim="fullName"
+                        type="text"
+                        class="form-control"
+                        placeholder="Nguyễn Văn A"
+                    />
+                    <div v-if="fullNameError" class="text-danger small mt-1">{{ fullNameError }}</div>
+                  </div>
+
+                  <div class="mb-3">
+                    <label class="form-label">Số điện thoại <span class="text-danger">*</span></label>
+                    <input
+                        v-model="phone"
+                        type="text"
+                        class="form-control"
+                        placeholder="098xxxxxxx"
+                        @input="handlePhoneInput"
+                    />
+                    <div v-if="phoneError" class="text-danger small mt-1">{{ phoneError }}</div>
+                  </div>
+
+                  <div class="mb-3">
+                    <label class="form-label">Giới tính</label>
+                    <select v-model="gender" class="form-select">
+                      <option :value="true">Nam</option>
+                      <option :value="false">Nữ</option>
+                    </select>
+                  </div>
+                </div>
+              </div>
+            </div>
+
+            <div class="col-md-4">
+              <div class="card border-0 shadow-sm h-100">
+                <div class="card-body d-flex flex-column align-items-center text-center">
+                  <div class="avatar-wrapper mb-3">
+                    <img :src="avatarPreview" alt="Avatar" class="avatar-preview" />
+                  </div>
+                  <div class="w-100">
+                    <label class="form-label">Avatar</label>
+                    <input
+                        type="file"
+                        class="form-control"
+                        accept="image/jpeg,image/png,image/webp"
+                        @change="onAvatarChange"
+                    />
+                    <div v-if="avatarError" class="text-danger small mt-1">{{ avatarError }}</div>
+                  </div>
+                </div>
+              </div>
+            </div>
+          </div>
+
+          <div class="card border-0 shadow-sm mt-4">
+            <div class="card-body">
+              <h6 class="fw-semibold mb-3 text-primary">
+                <i class="fas fa-map-marker-alt me-2"></i>
+                Địa chỉ
+              </h6>
+
+              <div class="row g-3">
+                <div class="col-md-6">
+                  <label class="form-label">Tỉnh / Thành phố <span class="text-danger">*</span></label>
+                  <select v-model="province" class="form-select">
+                    <option value="">Chọn tỉnh / thành phố</option>
+                    <option v-for="p in provinces" :key="p.code" :value="p.name">{{ p.name }}</option>
+                  </select>
+                  <div v-if="provinceError" class="text-danger small mt-1">{{ provinceError }}</div>
+                </div>
+
+                <div class="col-md-6">
+                  <label class="form-label">Phường / Xã <span class="text-danger">*</span></label>
+                  <select v-model="ward" class="form-select" :disabled="!province">
+                    <option value="">Chọn phường / xã</option>
+                    <option v-for="w in wards" :key="w.code" :value="w.name">{{ w.name }}</option>
+                  </select>
+                  <div v-if="wardError" class="text-danger small mt-1">{{ wardError }}</div>
+                </div>
+
+                <div class="col-md-6">
+                  <label class="form-label">Địa chỉ chi tiết <span class="text-danger">*</span></label>
+                  <input
+                      v-model.trim="detailAddress"
+                      type="text"
+                      class="form-control"
+                      placeholder="123 Lê Lợi"
+                  />
+                  <div v-if="detailAddressError" class="text-danger small mt-1">{{ detailAddressError }}</div>
+                </div>
+
+                <div class="col-md-6">
+                  <label class="form-label">Địa chỉ cũ</label>
+                  <input
+                      v-model.trim="oldAddress"
+                      type="text"
+                      class="form-control"
+                      placeholder="Nhập địa chỉ cũ (nếu có)"
+                  />
+                </div>
+              </div>
+            </div>
+          </div>
+        </div>
+
+        <div class="modal-footer border-0 pt-0">
+          <button type="button" class="btn btn-light" @click="handleClose">
+            <i class="fas fa-times me-2"></i>
+            Huỷ
+          </button>
+          <button type="button" class="btn btn-primary" :disabled="!isFormValid" @click="handleSubmit">
+            <i class="fas fa-check me-2"></i>
+            Tạo Host
+          </button>
+        </div>
+      </div>
+    </div>
+    <div class="modal-backdrop fade show"></div>
+  </div>
+</template>
+
+<script setup>
+import { computed, onBeforeUnmount, onMounted, ref, watch } from "vue";
+import provinces from "/src/assets/js/address.json";
+
+const emit = defineEmits(["close", "created"]);
+
+const email = ref("");
+const fullName = ref("");
+const phone = ref("");
+const gender = ref(true);
+const province = ref("");
+const ward = ref("");
+const detailAddress = ref("");
+const oldAddress = ref("");
+
+const avatarFile = ref(null);
+const avatarPreview = ref("https://via.placeholder.com/160x160?text=Avatar");
+const avatarError = ref("");
+
+const selectedProvince = computed(() => provinces.find((p) => p.name === province.value));
+const wards = computed(() => selectedProvince.value?.wards || []);
+
+watch(province, () => {
+  ward.value = "";
+});
+
+const emailError = computed(() => {
+  if (!email.value) return "Email bắt buộc";
+  const emailRegex = /^[\w-.]+@([\w-]+\.)+[\w-]{2,}$/;
+  return emailRegex.test(email.value) ? "" : "Email không hợp lệ";
+});
+
+const fullNameError = computed(() => {
+  return fullName.value ? "" : "Họ và tên bắt buộc";
+});
+
+const phoneError = computed(() => {
+  if (!phone.value) return "Số điện thoại bắt buộc";
+  if (!/^\d+$/.test(phone.value)) return "SĐT chỉ được chứa số";
+  return phone.value.length >= 9 && phone.value.length <= 11
+      ? ""
+      : "Độ dài SĐT phải từ 9-11 số";
+});
+
+const provinceError = computed(() => {
+  return province.value ? "" : "Vui lòng chọn tỉnh/thành";
+});
+
+const wardError = computed(() => {
+  return ward.value ? "" : "Vui lòng chọn phường/xã";
+});
+
+const detailAddressError = computed(() => {
+  return detailAddress.value ? "" : "Địa chỉ chi tiết bắt buộc";
+});
+
+const isFormValid = computed(() => {
+  return (
+      !emailError.value &&
+      !fullNameError.value &&
+      !phoneError.value &&
+      !provinceError.value &&
+      !wardError.value &&
+      !detailAddressError.value &&
+      !avatarError.value
+  );
+});
+
+const handlePhoneInput = (event) => {
+  const onlyDigits = event.target.value.replace(/\D/g, "");
+  phone.value = onlyDigits;
+};
+
+const onAvatarChange = (event) => {
+  const file = event.target.files?.[0];
+  avatarError.value = "";
+
+  if (!file) {
+    avatarFile.value = null;
+    avatarPreview.value = "https://via.placeholder.com/160x160?text=Avatar";
+    return;
+  }
+
+  const allowedTypes = ["image/jpeg", "image/png", "image/webp"];
+  const maxSize = 2 * 1024 * 1024;
+
+  if (!allowedTypes.includes(file.type)) {
+    avatarError.value = "Chỉ chấp nhận ảnh JPG, PNG, WEBP";
+    avatarFile.value = null;
+    avatarPreview.value = "https://via.placeholder.com/160x160?text=Avatar";
+    return;
+  }
+
+  if (file.size > maxSize) {
+    avatarError.value = "Kích thước ảnh tối đa 2MB";
+    avatarFile.value = null;
+    avatarPreview.value = "https://via.placeholder.com/160x160?text=Avatar";
+    return;
+  }
+
+  avatarFile.value = file;
+  avatarPreview.value = URL.createObjectURL(file);
+};
+
+const handleClose = () => {
+  emit("close");
+};
+
+const handleSubmit = () => {
+  if (!isFormValid.value) return;
+
+  const payload = {
+    email: email.value,
+    fullName: fullName.value,
+    phone: phone.value,
+    gender: gender.value,
+    address: `${detailAddress.value}/!!${ward.value}/!!${province.value}`,
+    oldAddress: oldAddress.value,
+    avatarFile: avatarFile.value
+  };
+
+  console.log("CREATE HOST PAYLOAD:", payload);
+  emit("created");
+  emit("close");
+};
+
+const handleEsc = (event) => {
+  if (event.key === "Escape") {
+    handleClose();
+  }
+};
+
+onMounted(() => {
+  window.addEventListener("keydown", handleEsc);
+});
+
+onBeforeUnmount(() => {
+  window.removeEventListener("keydown", handleEsc);
+});
+</script>
+
+<style scoped>
+.modal-backdrop {
+  z-index: 1040;
+}
+
+.modal {
+  z-index: 1050;
+}
+
+.card {
+  border-radius: 12px;
+}
+
+.avatar-wrapper {
+  width: 160px;
+  height: 160px;
+  border-radius: 50%;
+  overflow: hidden;
+  background: #f8fafc;
+  border: 1px dashed #d1d5db;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+}
+
+.avatar-preview {
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+}
+</style>
diff --git a/src/components/host/Test.vue b/src/components/host/Test.vue
index db0e552064a10943f618d4174602ec174d18b7d2..20e76c6ad03b82762aa922d2702f1cc5d968c11d 100644
--- a/src/components/host/Test.vue
+++ b/src/components/host/Test.vue
@@ -1,32 +1,36 @@
 <template>
   <div class="container-fluid py-4 host-list-container">
     <!-- HEADER -->
     <div class="d-flex justify-content-between align-items-center mb-4">
       <div>
         <h4 class="mb-1 fw-bold text-dark">Danh sách Host</h4>
         <p class="text-muted mb-0">Quản lý thông tin các chủ nhà trong hệ thống</p>
+        <button class="btn btn-primary btn-sm mt-3" @click="showCreateHostModal = true">
+          <i class="fas fa-plus me-2"></i>
+          + Thêm Host
+        </button>
       </div>
       <div class="text-end">
         <span class="badge bg-light text-dark border">
           <i class="fas fa-users me-1"></i>
           Tổng: <span class="fw-bold">{{ totalRecords }}</span> host
         </span>
       </div>
     </div>

     <!-- FILTER SECTION -->
     <div class="card shadow-sm border-0 mb-4 filter-section filter-purple">
     <div class="card-header bg-transparent border-0 py-3">
         <h6 class="mb-0 fw-semibold">
           <i class="fas fa-filter me-2 text-primary"></i>
           Bộ lọc tìm kiếm
         </h6>
       </div>
       <div class="filter-full-width">
         <div class="card-body pt-0">
         <div class="row g-3">
           <!-- Search -->
           <div class="col-xl-4 col-lg-4 col-md-6">
             <i class="fas fa-magnifying-glass text-primary me-1"></i>
             <label class="form-label fw-medium small">Tìm kiếm</label>
             <div class="input-group input-group-sm">
@@ -295,57 +299,63 @@
                 title="Trang sau"
                 @click="pageNo++"
             >
               <i class="fas fa-chevron-right"></i>
             </button>

             <!-- LAST PAGE -->
             <button
                 class="btn btn-outline-secondary"
                 :disabled="pageNo >= totalPages - 1"
                 title="Trang cuối"
                 @click="goLastPage"
             >
               <i class="fas fa-angles-right"></i>
             </button>
           </div>

         </div>
       </div>
     </div>
     <HostDetailModal
         v-if="showDetail"
         :host="selectedHost"
         @close="showDetail = false"
     />
+    <CreateHostModal
+        v-if="showCreateHostModal"
+        @close="showCreateHostModal = false"
+        @created="fetchData"
+    />
   </div>
 </template>

 <script setup>
 import { ref, computed, watch, onMounted } from "vue";
 import api from "/src/api/api.js";
 import provinces from "/src/assets/js/address.json";
+import CreateHostModal from "./CreateHostModal.vue";

 const data = ref([]);
 const loading = ref(false);
 const totalRecords = ref(0);
 const totalPages = ref(1);

 const pageNo = ref(0);
 const pageSize = ref(10);

 const goFirstPage = () => {
   if (pageNo.value > 0) {
     pageNo.value = 0;
   }
 };

 const goLastPage = () => {
   if (pageNo.value < totalPages.value - 1) {
     pageNo.value = totalPages.value - 1;
   }
 };

 const filters = ref({
   search: null,
   gender: null,
   status: null,
@@ -359,50 +369,52 @@ const selectedProvince = computed(() =>

 // Computed properties
 const totalPagesComputed = computed(() => {
   return Math.ceil(totalRecords.value / pageSize.value);
 });

 // Methods
 const getInitials = (name) => {
   if (!name) return '?';
   return name
       .split(' ')
       .map(n => n[0])
       .join('')
       .toUpperCase()
       .substring(0, 2);
 };

 const formatAddress = (address) => {
   if (!address) return "Chưa cập nhật";

   return address
       .replace(/^\d+\/!!/, "")
       .replace(/\/!!/g, ", ");
 };

+const showCreateHostModal = ref(false);
+
 let searchTimeout = null;
 watch(
     () => ({ ...filters.value }),
     (newFilters, oldFilters) => {
       // reset page khi filter đổi
       pageNo.value = 0;

       // debounce riêng cho search
       if (newFilters.search !== oldFilters?.search) {
         clearTimeout(searchTimeout);
         searchTimeout = setTimeout(() => {
           fetchData();
         }, 400); // 👈 300–500ms là đẹp
       } else {
         fetchData();
       }
     },
     { deep: true }
 );
 watch(
     () => filters.value.tinh,
     (newTinh, oldTinh) => {
       if (newTinh !== oldTinh) {
         filters.value.phuong = ''; // 👈 luôn đưa về "Tất cả phường/quận"
       }
